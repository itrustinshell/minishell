Gestione dei memory leaks

typedef struct s_cmd
{
	char				*cmd;
	char				**args;
	char				*path;
	struct s_cmd		*next;
	t_redir				*redirlist;
	int					argc;
}	t_cmd;

freestr(cmd);
freematrix(args);
free(path);
freecmd(next);
freeredir(redirlist);
